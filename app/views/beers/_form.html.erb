<%= simple_form_for(@beer, remote: request.xhr?, html: { data: { modal: true }, multipart: true}) do |f| %>
  <% if @beer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
      <% @beer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.input :name %>
    <%= f.input :description%>
    <%= f.input :image, as: :file %>
    <%= f.submit 'Add'%>
  </div>
<% end %>
