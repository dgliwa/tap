angular.module('beerApp.services').factory('beerModal', function (btfModal) {
      return btfModal({
              controller: 'BeerModalController',
                 controllerAs: 'vm',
                 templateUrl: '<%= asset_path("beerModal.html") %>'
            });
});
