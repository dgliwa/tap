angular.module("beerApp.controllers").controller("BeerController", ["$routeParams", "$window", "$filter", "$q", "$anchorScroll", function($routeParams, $window, $filter, $q, $anchorScroll) {
    var vm = this;
}]);

